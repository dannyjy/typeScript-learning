// const traficPoll = document.getElementById("traficLightPoll") as HTMLElement
// const timer = (fun: () => void,time: number) => setInterval(fun,time)
// traficPoll.addEventListener('click',() => {})

enum TraficLight{
    RED = 10,
    GREEN,
    YELLOW
}

console.log(TraficLight.GREEN)